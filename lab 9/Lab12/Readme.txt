Lab 12 — Абстрактные классы и интерфейсы в Python

 Цель лабораторной работы
Познакомиться с концепциями абстракции, интерфейсов и полиморфизма в объектно-ориентированном программировании на Python с использованием модуля `abc`.  
Научиться создавать абстрактные классы, переопределять методы в дочерних классах и реализовывать интерфейс-подобные структуры.

---

# 📌 Содержание лабораторной работы

 ✔ Задание 1 — Абстрактный класс Vehicle
**Цель:** создать абстрактный класс с обязательными методами и реализовать наследников.

Реализовать:

- `Vehicle` — абстрактный класс
- `Car` — наследник
- `Bicycle` — наследник

Методы:

- `move()`
- `fuel_type()`

---

✔ Задание 2 — Интерфейс-подобный класс DatabaseInterface
Реализовать абстрактный класс:

- `connect()`
- `execute_query()`
- `close()`

Создать классы:

- `SQLiteDatabase`
- `PostgreSQLDatabase`

Оба класса должны возвращать имитацию работы базы данных.


---

Задание 3 — Абстрактный класс Logger
Создать абстрактный класс:

- `log_info(message)`
- `log_error(message)`

Реализации:

- `ConsoleLogger` — выводит в консоль
- `FileLogger` — пишет в файл logs.txt


---

 ✔ Задание 4 — Полиморфизм на примере фигур
Создать абстрактный класс `Shape` и наследников:

- `Circle`
- `Rectangle`
- `Triangle`

Каждый реализует метод `area()`.

Создать список фигур и вывести площади с помощью полиморфизма.



---

 ✔ Задание 5 — Абстрактная платежная система
Создать абстрактный класс `PaymentSystem`:

- `authorize(amount)`
- `process_payment(amount)`
- `refund(amount)`

Реализовать:

- `CreditCardPayment`
- `PayPalPayment`
